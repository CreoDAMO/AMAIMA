# ============================================================================
# AMAIMA Android — gradle/wrapper/gradle-wrapper.properties
# ============================================================================
# CURRENT (your existing state):
#   distributionUrl=https\://services.gradle.org/distributions/gradle-8.14.2-bin.zip
#
# FINAL TARGET (Phase 5 — use this file):
#   Gradle 9.3.1 — required by AGP 9.0.1 (min 9.1.0, we use 9.3.1 for latest fixes)
#
# INTERMEDIATE (Phase 3 — use temporarily while on AGP 8.7.x):
#   distributionUrl=https\://services.gradle.org/distributions/gradle-8.14.2-bin.zip
#   (keep 8.14.2 until AGP 8.7 is stable in your project, then jump to 9.3.1)
#
# IMPORTANT: After changing this file, run:
#   ./gradlew wrapper --gradle-version=9.3.1
# This updates the wrapper JAR in gradle/wrapper/ as well.
# ============================================================================

distributionBase=GRADLE_USER_HOME
distributionPath=wrapper/dists

# Phase 5 — Final target (swap to this after Phase 3 is verified):
distributionUrl=https\://services.gradle.org/distributions/gradle-9.3.1-bin.zip

zipStoreBase=GRADLE_USER_HOME
zipStorePath=wrapper/dists

# Gradle 9.x: The wrapper is now an executable JAR.
# Do not delete or gitignore gradle-wrapper.jar — it is required.
