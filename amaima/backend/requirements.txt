# AMAIMA Backend Dependencies

# Core Framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6
pydantic>=2.5.0
pydantic-settings>=2.1.0

# Database
asyncpg>=0.29.0
sqlalchemy[asyncio]>=2.0.25
psycopg2-binary>=2.9.9

# Redis (optional — used for distributed rate limiting)
redis>=5.0.0

# Authentication
python-jose[cryptography]>=3.3.0
bcrypt>=4.0.0

# SSE Streaming
sse-starlette>=1.6.0

# System monitoring (used by unified_smart_router DeviceCapability)
psutil>=5.9.0

# Configuration & Logging
pyyaml>=6.0.1
python-dotenv>=1.0.0

# HTTP Client (used by audio_service, image_service, video_service, fhe router)
httpx>=0.26.0
aiofiles>=23.2.1

# Image format conversion (used by image_service.py — jpeg/webp output)
Pillow>=10.0.0

# Utilities
python-dateutil>=2.8.2
orjson>=3.9.0

# FHE — Fully Homomorphic Encryption (Microsoft SEAL via TenSEAL)
# ---------------------------------------------------------------
# The backend Dockerfile (amaima/backend/Dockerfile) strips this line and
# builds TenSEAL from source against Intel HEXL + SEAL v4.1.2 for AVX-512
# acceleration. The source build delivers ~3-4x faster NTT vs this wheel.
#
# For local development without the 3-stage Docker build:
#   pip install tenseal  — installs a generic pre-built wheel (no AVX-512)
#
# For local development without TenSEAL at all:
#   Set FHE_ENABLED=false — FHE endpoints return 503, everything else works.
#
# Do NOT remove this line — it is the fallback for environments that cannot
# run the 3-stage Docker build (Replit, CI, bare pip installs).
tenseal>=0.3.15

# Testing
pytest>=9.0.0
pytest-asyncio>=0.23.0
pytest-cov>=4.0.0

# Linting & Formatting (used by Makefile lint/format targets)
ruff>=0.4.0
