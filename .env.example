# =============================================================================
# AMAIMA - Environment Variables
# =============================================================================
# Copy this file to .env and fill in your values.
# Do NOT commit .env to version control.

# =============================================================================
# Database (Required for production)
# =============================================================================
# PostgreSQL connection string. Required for API key management, usage tracking,
# billing, conversation history, and MAU rate limiting.
DATABASE_URL=postgresql://user:password@host:5432/dbname

# Used by docker-compose for the local PostgreSQL container password.
# DB_PASSWORD=your-strong-db-password

# =============================================================================
# NVIDIA NIM API (Required for AI inference)
# =============================================================================
# Get your key at https://build.nvidia.com
NVIDIA_API_KEY=nvapi-xxxxxxxxxxxxxxxxxxxxxxxxxxxx

# NIM API Endpoints (defaults shown, override if using custom endpoints)
# COSMOS_NIM_URL=https://integrate.api.nvidia.com/v1
# BIONEMO_NIM_URL=https://integrate.api.nvidia.com/v1

# =============================================================================
# Security (Required for production)
# =============================================================================
# Protects API endpoints. MUST be changed from default in production —
# the default value is publicly known and would let anyone use your NVIDIA credits.
API_SECRET_KEY=your-api-secret-key-here

# =============================================================================
# Application Configuration
# =============================================================================
# Set to "execution-enabled" for real AI responses via NVIDIA NIM.
AMAIMA_EXECUTION_MODE=execution-enabled

# How the frontend reaches the backend. Defaults to http://localhost:8000
# which is correct for full-stack (single-container) deployment.
# Only change this if running frontend and backend as separate services.
BACKEND_URL=http://localhost:8000

# Frontend port (default: 5000)
# PORT=5000

# =============================================================================
# Stripe (Optional - for billing/subscriptions)
# =============================================================================
# On Replit: Managed automatically by the Stripe integration — no manual config needed.
# Elsewhere: Get keys from https://dashboard.stripe.com/apikeys
# STRIPE_SECRET_KEY=sk_test_xxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Webhook signing secret for verifying Stripe webhook events.
# Set this after configuring your webhook endpoint in the Stripe dashboard
# (endpoint: https://YOUR_URL/api/stripe/webhook).
# STRIPE_WEBHOOK_SECRET=whsec_xxxxxxxxxxxxxxxxxxxxxxxxxxxx
